# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# Model Configuration
# Configure which OpenAI models to use for different tasks

# Model for processing individual flow chunks
CHUNK_PROCESSING_MODEL=gpt-5-mini

# Model for generating creative summaries
SUMMARY_MODEL=gpt-5

# Model for generating social media images (only gpt-image-1 is supported)
IMAGE_MODEL=gpt-image-1
